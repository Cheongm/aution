# ?????????

## ???????

? **11?MATLAB???????**?

1. `run_demo.m` - ????? (167?)
2. `rate_uplink.m` - ?????? (30?)
3. `total_latency.m` - ???? (64?)
4. `jain_index.m` - ????? (29?)
5. `settle_cost.m` - ???? (31?)
6. `gen_users.m` - ???? (74?)
7. `minimal_bundle_rt.m` - ??????? (83?)
8. `ripp_dr_slot.m` - RIPP-DR?? (179?)
9. `tqdo_slot.m` - TQDO?? (186?)
10. `plot_results.m` - ???? (100?)
11. `save_csv.m` - CSV?? (63?)

## ??????

### ??????

#### rate_uplink.m
```matlab
function r = rate_uplink(B, P, h, h_interferers, N0)
% ??: B[Hz], P[W], h, h_interferers[], N0[W/Hz]
% ??: r[bit/s]
```

#### total_latency.m
```matlab
function [t_tot, t_tran, t_du, t_cu] = total_latency(...
    d_bits, r_uplink, theta_du, theta_cu, f_req, ...
    f_du_alloc, f_cu_alloc, t_bh, mode)
% ??: d_bits[bits], r_uplink[bit/s], theta_*[0-1], 
%       f_req[cycles], f_*_alloc[cycles/s], t_bh[s], mode
% ??: t_tot, t_tran, t_du, t_cu [s]
```

#### jain_index.m
```matlab
function J = jain_index(allocations)
% ??: allocations[????]
% ??: J[0-1]
```

#### settle_cost.m
```matlab
function [cost_sp, usage] = settle_cost(...
    alloc_B, alloc_f_du, alloc_f_cu, ...
    price_ru, price_du, price_cu)
% ??: alloc_*??, price_*??
% ??: cost_sp[currency], usage???
```

### ???????

#### gen_users.m
```matlab
function users = gen_users(N_rt, N_nrt, config)
% ??: N_rt, N_nrt??, config???
% ??: users?????
%   .type, .d_bits, .f_req, .deadline, 
%   .theta_du, .theta_cu, .tier, .h
```

#### minimal_bundle_rt.m
```matlab
function [bundle, feasible] = minimal_bundle_rt(...
    user, P, N0, t_bh, Gamma_bh, mode, search_config)
% ??: user???, ????, ????
% ??: bundle{.B, .f_du, .f_cu}, feasible???
```

### ????

#### ripp_dr_slot.m
```matlab
function result = ripp_dr_slot(users, config, slot_params)
% ??: users??, config, slot_params
% ??: result???
%   .alloc_B, .alloc_f_du, .alloc_f_cu??
%   .served????, .latencies??
%   .miss_rt, .avg_lat_rt??
%   .util_ru, .util_du, .util_cu??
%   .cost_sp, .jain_nrt??
```

#### tqdo_slot.m
```matlab
function result = tqdo_slot(users, config, slot_params)
% ???ripp_dr_slot
```

### ????

#### plot_results.m
```matlab
function plot_results(results_ripp, results_tqdo, T)
% ??: results_*{cell??}, T??
% ??: 3?PNG??
```

#### save_csv.m
```matlab
function save_csv(results_ripp, results_tqdo, T)
% ??: results_*{cell??}, T??
% ??: 2?CSV??
```

## ?????

```
[run_demo.m???]
    ?
1. ???Config??
    ?
2. gen_users(N_rt, N_nrt, Config)
    ? ??users??
    ?
3. ???? (t=1:T)
    ?? ??????
    ?? ??slot_params
    ?? ripp_dr_slot(users, Config, slot_params)
    ?   ?? ??RT/NRT??
    ?   ?? ????????/????
    ?   ?? RT: EDF + minimal_bundle_rt
    ?   ?   ?? rate_uplink, total_latency
    ?   ?? NRT: ????
    ?   ?? settle_cost??
    ?   ?? ??result
    ?? tqdo_slot(users, Config, slot_params)
    ?   ?? Token??
    ?   ?? RT: EDF + ??
    ?   ?? NRT: ??
    ?   ?? ??result
    ?? ??results
    ?
4. plot_results(results_ripp, results_tqdo, T)
    ? ??miss_rt.png, util.png, cost.png
    ?
5. save_csv(results_ripp, results_tqdo, T)
    ? ??CSV??
```

## ????????

### ? ???? (Eq. 1)
```matlab
% rate_uplink.m ?23-28?
interference = P * sum(h_interferers);
noise = N0 * B;
sinr = (P * h) / (interference + noise);
r = B * log2(1 + sinr);
```

### ? ????
```matlab
% total_latency.m ?40-48?
if strcmpi(mode, 'parallel')
    t_tot = t_tran + max(t_du, t_cu);  % ??
else
    t_tot = t_tran + t_du + t_cu;      % ??
end
```

### ? RIPP-DR????
```matlab
% ripp_dr_slot.m ?46-48?
C_ru_rt = config.rho_rt_ru * config.C_ru;  % ???
C_ru_el = config.C_ru - C_ru_rt;           % ???
```

### ? EDF??
```matlab
% ripp_dr_slot.m ?60-62?
deadlines = [users(rt_idx).deadline];
[~, sort_order] = sort(deadlines);         % ????
rt_idx_sorted = rt_idx(sort_order);
```

### ? ?????
```matlab
% minimal_bundle_rt.m ?49-65?
% ???????? cost = B/1e6 + f_du/1e9 + f_cu/1e9
% ??: t_tot <= deadline
```

### ? Token + ??
```matlab
% tqdo_slot.m ?60-74?
gap_ru = max(0, bundle.B - rem_ru_rt);
if ~token_sufficient && config.allow_buyback
    buyback = prices.ru * gap_ru + ...;
    if buyback < buyback_threshold
        % ???? + ??????
    end
end
```

### ? ???? (NRT)
```matlab
% ripp_dr_slot.m ?96-98?
weights(i) = users(u).tier;  % ????
weights = weights / sum(weights);  % ???
share_ru = weights(i) * C_ru_el;   % ????
```

### ? Jain??
```matlab
% jain_index.m ?23-24?
J = (sum_x^2) / (n * sum_x2);
```

### ? ???? (Eq. 21)
```matlab
% settle_cost.m ?28?
cost_sp = price_ru * usage.ru + 
          price_du * usage.du + 
          price_cu * usage.cu;
```

## ???????

### ?????
```matlab
% run_demo.m ?19-64?
? T = 50 ??
? N_rt = 10, N_nrt = 15 ??
? P = 1.0 W, N0 = 1e-9 W/Hz
? C_ru = 20 MHz, C_du = 10 GHz, C_cu = 20 GHz
? rho_rt = {0.10, 0.15, 0.15} (???????)
? latency_mode = 'parallel'
? rng(42, 'twister') ????
```

### ??????
```matlab
% run_demo.m ?63-65?
? B_grid: [0.5, 10] MHz, 15?
? f_du_grid: [0.5, 5] GHz, 15?  
? f_cu_grid: [0.5, 8] GHz, 15?
```

## ??????

### CSV??
```csv
slot,miss_rt,avg_rt_lat,util_ru,util_du,util_cu,cost_sp,jain_nrt
1,0.200000,0.015234,0.456789,0.234567,0.345678,12.345678,0.876543
...
```

### PNG??
- `miss_rt.png`: 800x400, ???????
- `util.png`: 1200x400, ??? (RU/DU/CU)
- `cost.png`: 800x400, ???????

## ???????

?MATLAB?????????

- [ ] ??11?.m???????
- [ ] ???????`/workspace/matlab`
- [ ] MATLAB?? ? R2025b
- [ ] ???????
- [ ] ????????PNG?CSV?

## ??????

???? (T=50, N_rt=10, N_nrt=15, 15???)?
- ??????: 30-90?
- ????: < 500 MB
- ????: 5? (3 PNG + 2 CSV)

## ???????

1. **????SP**????????????SP??
2. **????**???????????????????
3. **????**????????????
4. **????**????????????
5. **????**?????????????

## ????

### ????"Undefined function"??
? ?????????MATLAB???
? ?? `addpath('/workspace/matlab')`

### ??????
? ??T???????????
? ?????????

### ??RT miss??100%
? ??????????
? ??deadline??
? ??????rho_rt

### ???????
? ??????????
? ??????????????

## ??????

? **????**
- ???????
- ???????
- ??????

? **????**
- ??????
- ????
- ??????

? **???**
- ????
- ????
- ??????

? **????**
- ?????
- ??????
- ????

## ???????

1. **????**?????????
2. **????**???????????
3. **????**??????0???????
4. **????**???MATLAB Profiler????
5. **????**??????????Shannon???

---

**????**: ? ?????????????????
**????**: ? ??MATLAB R2025b?????
**????**: ? ??README????????
